# ─── Produção ────────────────────────────────────────────────────────────────
# Exporte a variável no seu shell (recomendado), ou copie para .env:
#
#   export POSTGRES_PASSWORD=sua_senha_forte
#
# Gere uma senha forte com:
#   openssl rand -base64 32 | tr -d '=+/' | cut -c1-32
#
# Os tiles são servidos pelo Caddy via proxy /tiles/* → martin:7800 com HTTPS.

POSTGRES_PASSWORD=troque_por_uma_senha_forte

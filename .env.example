# ─── Produção ────────────────────────────────────────────────────────────────
# Copie para .env e ajuste antes de executar: docker compose up
#
# Hostname público do servidor (IP ou domínio).
# Usado para montar a URL direta do Martin no build da webapp.
# Exemplo: embarcacoes.msousa.net  ou  192.168.0.10
SERVER_HOST=localhost

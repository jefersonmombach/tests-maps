#!/bin/bash
# pg_tileserv configuration file example
# This file demonstrates available configuration options for pg_tileserv

# Database connection parameters
# DATABASE_URL is set via docker-compose environment variables

# Server configuration
LISTEN_PORT=7800
# LISTEN_HOST=0.0.0.0

# Enable/disable features
# TILE_CACHE_SIZE=0  # Cache size in MB (0 = disabled)
# LAYER_QUERY_LIMIT=1000000

# CORS configuration
# CORS_ALLOWED_ORIGINS=*

# Logging
# LOG_LEVEL=info

# Optional: Custom root path for the API
# ROOT_PATH=/tiles

# Martin tile server configuration
# https://maplibre.org/martin/
#
# listen_addresses and the database connection string are passed
# via CLI arguments in docker-compose, so no DB config is needed here.

listen_addresses: '0.0.0.0:7800'

# Tile generation defaults
keep_alive: 75
worker_processes: 8

postgres:
  # Reference env var explicitly; Martin expands this from container env.
  connection_string: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/tilemaps}
  auto_publish:
    from_schemas:
      - public
